{
  "name": "Ichor",
  "version": "0.0.0",
  "scripts": {
    "typecheck": "tsc",
    "start": "parcel index.html --no-hmr",
    "build": "parcel build index.html",
    "clean": "rm -r .cache dist",
    "test": "ts-node node_modules/.bin/tape src/**/*.spec.ts | faucet"
  },
  "devDependencies": {
    "@types/tape": "^4.2.33",
    "faucet": "^0.0.1",
    "parcel-bundler": "^1.11.0",
    "tape": "^4.9.2",
    "ts-node": "^8.0.2",
    "typescript": "^3.2.4"
  },
  "dependencies": {
    "permissive-fov": "^2.0.0",
    "prng": "^0.0.1"
  },
  "browserslist": [
    "last 1 Chrome versions"
  ]
}
